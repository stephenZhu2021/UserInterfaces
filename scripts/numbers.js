for( let i = 0; i <= 30; i++ ){
	/*
	if ( ( i % 2 ) == 1 )
		console.log( i + " is odd.");
	else 
		console.log( i + " is even.");
	*/
	console.log( i + ((i % 2) ? " is odd" : " is even" ) );

}

/*1. Create a new file named numbers.js

2. Write a loop that counts between 0 and 30.

3. For each number check if the number is odd or even.

4. Output the result to the screen.

	0 is even
	1 is odd
*/